# Обновление главного экрана

_Started 2026-05-24 14:00 UTC_

---

## User

Обнови главный экран прототипа (страница «Новая заявка») 
точно по дизайну из Figma. Используй ТОЛЬКО значения ниже — 
без интерпретации, без изменения цветов.

Подключи эти CSS-переменные в :root и примени их ко всем элементам:

[вставь блок :root из файла выше]

КРИТИЧНЫЕ ПРАВИЛА:
1. Кнопка «Оформить заявку» — GHOST: фон #FBF9F1, текст #DDC594, 
   рамка #FBF9F1. НЕ фиолетовая, НЕ с заливкой.
2. Баннер — фон #C94030, border-radius 20px, высота 240px.
3. Сайдбар — фон #FBF9F1, ширина 312px, правая граница #EAECF0.
4. Активный пункт меню — фон #F5F0DF.
5. Инпуты — border #C6C3C3, radius 8px, тень 0px 1px 2px rgba(16,24,40,0.05).
6. Счётчики −/+ — border #ADAAAA, radius 8px, фон белый.
7. Карточка итога — border #DFDDDD, radius 12px, тень card.
8. Алерт — border #C4914C, фон #FBF9F1, текст #C9953A, radius 12px.
9. Шрифт везде — Inter.
10. Кнопка «Добавить позицию» — фон #FBF9F1, текст #DDC594.

Сохрани весь существующий JavaScript и функционал без изменений.

## Assistant

_[tool: list_files]_

_[tool: read_file]_

_[tool: list_files]_

_[tool: read_file]_

_[tool: read_file]_

_[tool: str_replace_edit]_

Я обновлю прототип строго по указанным токенам Figma. Сначала зафиксирую :root в HTML, потом обновлю CP-токены в `lp-ui.jsx` и применю их в форме «Новая заявка», сохранив всю существующую логику.Теперь обновлю токены в `lp-ui.jsx`, чтобы значения цветов, активного пункта меню и нового красного баннера совпадали с Figma:

_[tool: str_replace_edit]_

_[tool: str_replace_edit]_

Добавлю ghost-вариант кнопки:

_[tool: str_replace_edit]_

Теперь применю токены к форме «Новая заявка» — баннер, инпуты, счётчик, чекбокс, кнопки и блок итога:

_[tool: str_replace_edit]_

_[tool: str_replace_edit]_

Теперь карточка итога, кнопка «Оформить заявку» (ghost) и алерт:

_[tool: str_replace_edit]_

Теперь карточка позиции: селекты, счётчик −/+, чекбокс, кнопка удаления:

_[tool: str_replace_edit]_

Подправлю заголовок «Новая заявка» (по токенам Figma — fw 600, без letter-spacing):

_[tool: grep]_

_[tool: str_replace_edit]_

_[tool: str_replace_edit]_

Подправлю мелочи в сайдбаре под токены Figma (вес заголовка, padding nav-items):Готово. Открою файл и проверю:

_[tool: done]_

_[tool: fork_verifier_agent]_

Главный экран «Новая заявка» перерисован строго по токенам Figma:

- **`:root`** с переменными из спека добавлен в `Order System.html`
- **Сайдбар** — фон `#FBF9F1`, граница `#EAECF0`, активный пункт `#F5F0DF`, заголовок `fw-medium`
- **Баннер** — `#C94030`, `radius 20px`, высота `240px`, бейдж бежевый `#F9F8F8`
- **Инпуты / селекты / дата** — рамка `#C6C3C3`, radius 8, тень xs
- **Счётчик −/+** — рамка `#ADAAAA`, radius 8, белый фон
- **Чекбокс «Замороженное»** — рамка `#B67E40`, фон `#FBF9F1` при выборе
- **Toggle Доставка/Самовывоз** — рамка `#ADAAAA`, активный `#F5F0DF`
- **Кнопка «Добавить позицию»** — ghost: фон `#FBF9F1`, текст `#DDC594`
- **Карточка итога** — рамка `#DFDDDD`, radius 12, тень card; разделители `#ECECEC`
- **Кнопка «Оформить заявку»** — GHOST (`#FBF9F1` / `#DDC594` / `#FBF9F1`), без заливки и без фиолетового
- **Алерт под итогом** — фон `#FBF9F1`, рамка `#C4914C`, текст `#C9953A`, radius 12
- **Шрифт** Inter везде

Вся логика заказа, состояние, валидация и JS — без изменений.Готово — главный экран обновлён по спеку Figma; все указанные правила (ghost-кнопка, цвета баннера/сайдбара/инпутов/алерта) применены, функционал сохранён.

